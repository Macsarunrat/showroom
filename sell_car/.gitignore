# 1. ไฟล์ฐานข้อมูล (ห้ามเอาขึ้น เพราะข้อมูลเครื่องใครเครื่องมัน)
db.sqlite3

# 2. ไฟล์ขยะที่ Python สร้างเอง
__pycache__/
*.pyc

# 3. โฟลเดอร์เก็บรูปภาพ (หนัก และไม่ควรทับกัน)
media/

# 4. โฟลเดอร์รวมไฟล์ตกแต่ง (Server สร้างเองได้ผ่าน collectstatic)
staticfiles/

# 5. Environment (โปรแกรมที่ลงไว้ ใหญ่มาก ห้ามเอาขึ้น)
env/
venv/

# 6. ไฟล์ระบบของ Mac/Windows
.DS_Store
Thumbs.db